{
    "name": "ptp/stripe-payments",
    "description": "PTP Stripe Payments plugin for WordPress",
    "type": "wordpress-plugin",
    "require": {
        "php": ">=7.4",
        "stripe/stripe-php": "^13.0"
    },
    "autoload": {
        "psr-4": {
            "PTP\\Stripe\\": "src/"
        }
    }
}
